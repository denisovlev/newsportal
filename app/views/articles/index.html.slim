/= @articles.inspect
h1 
	- if @category
		| #{@category.name}
	- else 
		| Статьи

- if @articles.any?
	= render(partial: 'articles/article', collection: @articles)
- else 
	| There is no articles
= paginate @articles